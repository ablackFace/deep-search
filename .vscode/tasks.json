{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "spec:dashboard",
            "type": "shell",
            "command": "npx -y @pimzino/spec-workflow-mcp@latest --dashboard"
        }
    ]
}